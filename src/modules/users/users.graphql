type User {
  id: Int!
  username: String!
  email: String!
  roles: [Role!]!
}

enum Role {
  admin
  moderator
  user
}

type Query {
  currentUser: User
}

type Mutation {
  createUser(username: String!, password: String!, email: String!): User
}
